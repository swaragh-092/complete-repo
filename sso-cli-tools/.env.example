# ═══════════════════════════════════════════════════════════════════════════════
# SSO CLI Tools - Environment Configuration
# ═══════════════════════════════════════════════════════════════════════════════
# Copy this file to .env and adjust as needed
# All variables can also be set in sso-client.config.json
#
# Configuration priority:
# 1. Environment variables (SSO_*)
# 2. Config file (sso-client.config.json)
# 3. Default values shown below
# ═══════════════════════════════════════════════════════════════════════════════

# ─────────────────────────────────────────────────────────────────────────────
# PROTOCOL & DOMAIN
# ─────────────────────────────────────────────────────────────────────────────
# Protocol for all URLs (http or https)
SSO_PROTOCOL=https

# Base domain for all services
SSO_DOMAIN=local.test

# ─────────────────────────────────────────────────────────────────────────────
# DEPLOYMENT MODE
# ─────────────────────────────────────────────────────────────────────────────
# Docker mode: Use portless URLs (all traffic via nginx gateway)
# Set to 'true' when client apps run in Docker behind gateway
SSO_DOCKER_MODE=false

# Auth behind gateway: Use portless URLs for auth-service callbacks
# Set to 'true' (default) when auth-service is dockerized (most common)
# Set to 'false' only if running auth-service locally without gateway
SSO_AUTH_BEHIND_GATEWAY=true

# ─────────────────────────────────────────────────────────────────────────────
# SERVICE PORTS (only used when not behind gateway)
# ─────────────────────────────────────────────────────────────────────────────
# Auth service port (used when SSO_AUTH_BEHIND_GATEWAY=false)
SSO_AUTH_PORT=4000

# Account UI port (used when SSO_AUTH_BEHIND_GATEWAY=false)
SSO_ACCOUNT_UI_PORT=5174

# ─────────────────────────────────────────────────────────────────────────────
# API SETTINGS
# ─────────────────────────────────────────────────────────────────────────────
# API timeout in milliseconds
SSO_API_TIMEOUT=30000

# ═══════════════════════════════════════════════════════════════════════════════
# QUICK REFERENCE - Common Scenarios
# ═══════════════════════════════════════════════════════════════════════════════
#
# Scenario 1: Hybrid Mode (RECOMMENDED - default settings)
# - Auth in Docker, Client runs locally
# - SSO_DOCKER_MODE=false
# - SSO_AUTH_BEHIND_GATEWAY=true
#
# Scenario 2: Full Docker Mode
# - Everything in Docker behind gateway
# - SSO_DOCKER_MODE=true
# - SSO_AUTH_BEHIND_GATEWAY=true
#
# Scenario 3: Fully Local Mode
# - Everything runs locally with ports
# - SSO_DOCKER_MODE=false
# - SSO_AUTH_BEHIND_GATEWAY=false
# ═══════════════════════════════════════════════════════════════════════════════
